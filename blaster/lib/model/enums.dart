enum DonorStatus {
  BLOCKED,
  ACTIVE,
}

enum DonorCategory { PLATINUM, GOLD, SILVER, BRONZE }

enum BagStatus {
  REGISTERED,
  COLLECTED,
  TESTED,
  APPROVED,
  REJECTED,
  ISSUED,
  USED,
}

enum DonationRequestStatus {
  REQUESTED,
  DONATED,
}
